#ifndef _MOVIES_H_
#define _MOVIES_H_
#include <string>
#include <iostream>

class Movies
{
private:

public:

    // Constructor prototype
    Movies();

    // Copy Constructor prototype
    // Movie(const Movie &source);
   
    // Destructor prototype
    ~ Movies();

};

#endif // _MOVIES_H_